<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="repository.AccountRepository">
    <insert id="save" parameterType="entity.Account">
        insert into t_account(username,password,age) values(#{username},#{password},#{age})
    </insert>
    <delete id="delete" parameterType="long">
        delete from t_account where id=#{id}
    </delete>
    <select id="findAll" resultType="entity.Account">
        select * from t_account
    </select>
    <select id="findById" parameterType="entity.Account" resultType="entity.Account">
        select * from t_account where id=#{id}
    </select>
    <select id="findByName" parameterType="java.lang.String" resultType="entity.Account">
        select * from t_account where username=#{username}
    </select>
    <update id="update" parameterType="entity.Account">
        update t_account set username=#{username},password=#{password},age=#{age} where id=#{id}
    </update>
    <select id="findByidandname" resultType="entity.Account">
        select * from t_account where username=#{arg0} and age=#{arg1}
    </select>
</mapper>